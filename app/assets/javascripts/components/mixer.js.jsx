var Mixer = React.createClass({
  // getInitialState: function(){
  //   return{
  //     keys: Object.keys(BindStore.keyBindings()),
  //     currentTab: 'q'
  //   }
  // },

  render: function(){
    // var tabs = Object.keys(this.state.keys).map(function(key){
		// 	return <li key={key} onClick={function(){this.switchTab(key)}.bind(this)}>{TabNames[key]}</li>
		// }.bind(this))

    return (
      <div/>
    )
  }
});
